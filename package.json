{
  "name": "task-reminder-worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && tsc",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy",
    "db:init": "wrangler d1 execute task_reminder_db --local --file=./schema.sql"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^11.0.0",
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.15.0",
    "hono": "^4.5.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240401.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.50.0"
  }
}